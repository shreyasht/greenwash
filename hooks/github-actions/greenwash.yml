# greenwash CI check (FR-35). Stub — implemented at BUILD_PLAN.md §3 step 11.
# Copy to .github/workflows/ in the consuming repo.
name: greenwash
on:
  pull_request:
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # greenwash needs base..head
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      # - run: python -m greenwash --range "${{ github.event.pull_request.base.sha }}..${{ github.sha }}" --json
